# WorkBuddy 工作台 · 功能测试与改进意见

> 测试时间：2026-08-28  |  角色：UI Designer（黑盒 + 契约 + 运行测试）
> 测试对象：`index.html`（v2 精致版前端）+ `launcher.js`（Node 后端，混合健壮版）
> 测试方式：临时关闭 `autostartApps` 启动真实 `launcher.js`，对只读 API 逐一 curl 验证；端口探测；config 原始内容核对。

---

## 一、测试结果（实证）

| 项 | 结果 | 说明 |
|---|---|---|
| 服务启动 | ✅ | 监听 `127.0.0.1:38760`，`/api/config` 返回 200 |
| 应用列表加载 | ✅ | `apps=4`（workbuddy/ima/微信/企业微信），`pinned=2`，`files=2` |
| 本机应用搜索 | ✅ | `微信`→命中 6 项；`chrome`→命中 10 项；`building=false`（索引已就绪） |
| 统计端点 | ✅ | `/api/stat` 返回应用运行状态、端口 |
| 端口一致性 | ⚠️ | 当前 38760 空闲、与前端硬编码一致；但后端用 `findFreePort` 可能顺延（隐患见下） |
| 最近访问数据 | ❌ | **反斜杠全部丢失 + 乱码**，点击必然打不开（数据损坏） |

---

## 二、需求满足度对照

| 原始需求 | 实现情况 | 结论 |
|---|---|---|
| A. 双击 start.bat 拉起全部 + 全局开关 | `autostartApps` + launch-all/close-all | ✅ 逻辑满足（见 High#4 健壮性） |
| B. 搜索本机应用一键添加 | `/api/search` + addFoundApp | ✅ 工作正常 |
| C. 三栏列出 + 搜索修复 | 三栏面板 + 搜索 | ✅ |
| D. 文件夹打开无报警 | 后端环境变量传参打开 | ✅ 逻辑满足（前提：路径数据正确） |
| E. 在线文件标签卡 + 独立窗口 | tabs + `/popout.html` | ✅ |
| 8/21 设计：三栏暖色活力 | v2 精致版落地 | ✅ |

**结论：功能逻辑层面满足需求；但存在数据完整性 bug 与健壮性隐患，导致部分功能实际不可用。**

---

## 三、发现的问题（按严重度）

### 🔴 Critical（功能失效）
1. **`recentFolders` 数据损坏**
   - 实测响应：`"path":"C:UsersgfWorkBuddy6-08-17-13-53-15\report"`
   - 核对 `config.json` 原文确认：`\` 全丢失、`\2` 退化为乱码 ``。
   - 影响：「最近访问」点击无法打开任何文件夹。
2. **「企业微信」应用 path 损坏**
   - 当前：`"C:Program Files (x86)WXWorkWXWork.exe"`（缺 `:\` 与 `\`）
   - 影响：点击「打开」会失败。

### 🟠 High（健壮性 / 体验隐患）
3. **端口硬编码 vs 端口顺延冲突**
   - 前端 `BASE='http://127.0.0.1:38760'` 写死；后端 `findFreePort` 在 38760 被占用时顺延到 38761+。
   - 一旦端口冲突，前端**全部请求静默失败**，页面变死。
4. **`autostart` 无脑拉起全部**
   - 每次启动 2 秒后拉起所有应用，即使已在运行也会重复开窗口；「全部启动」按钮同理。
   - 建议：仅启动 `launchedPids` 未记录的；或启动前检测窗口/进程。
5. **打开文件夹的隐藏副作用**
   - `/api/open` 会把 target 写入 `recentFolders` 并 `saveConfig`——前端点击后「最近访问」列表不实时刷新（需手动刷新页）；且此写入绕过了前端的 `saveAndReload`。

### 🟡 Medium（交互一致性 / UX）
6. **行点击行为不一致**：文件夹整行可点打开，应用整行不可点（仅按钮）。建议统一为「整行点击=打开」。
7. **搜索无 loading 态**：首次索引构建期间（最长 120s）用户无感知，输入框无指示。
8. **缺少主动「固定文件夹」入口**：只能靠打开行为产生 recent，无法主动 pin（pinned 列表无法从 UI 增删）。
9. **无单应用关闭**：只有「全部关闭」，无法关单个。

### 🟢 Low（细节打磨）
10. popout 独立窗口是暗色 iframe，与暖色主题割裂，可继承主题。
11. 图标用 emoji，建议后续替换为 SVG 矢量（设计规范已留口）。
12. `/api/stat` 返回 `running` 标记，UI 未利用（可显示「运行中」状态点）。
13. 服务未启动时无友好提示（仅状态栏"加载失败"）。

---

## 四、改进意见与优先级

**P0（立即修，否则功能不可用）**
- 修复 `recentFolders` 损坏数据（建议：清空该字段，由用户使用自然重建；或按 `^([A-Za-z]):`→`$1:\` 启发式还原）。
- 修复「企业微信」path 为 `C:\Program Files (x86)\WXWork\WXWork.exe`。

**P1（健壮性，必须做）**
- 端口方案二选一：① 后端固定监听 38760（去掉 `findFreePort`）；② 后端把实际端口写入 `port.txt`，前端读取后动态拼 `BASE`。
- `autostart`/「全部启动」加去重（已运行的跳过）。

**P2（体验一致）**
- 应用整行可点击打开；搜索框 loading 指示；`openTarget` 成功后回调刷新「最近访问」。
- 增加「固定/取消固定文件夹」按钮。

**P3（打磨）**
- popout 继承主题；emoji→SVG；运行状态点；离线友好提示。

---

## 五、建议下一步
1. 先修 P0 两处数据损坏（需你确认是否允许我直接改 `config.json`）。
2. 再落地 P1 端口与去重，消除"偶发全失效"风险。
3. 按 P2 提升交互一致性。

> 注：本次测试已临时关闭 autostart 并在结束后还原 `config.json`（`autostartApps:true` 已恢复），未改动任何业务数据；`recentFolders`/企业微信 的损坏为既有数据问题。
