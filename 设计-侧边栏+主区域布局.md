# WorkBuddy 工作台 · 侧边栏 + 右侧主区域布局设计

## 一、设计目标

将当前「三栏并列」布局改为「左侧快捷侧边栏 + 右侧主工作区」的桌面式布局：

- **侧边栏**：开机即用，集中展示常用应用、常用文件夹、常用文件，点击即可启动/打开。
- **右侧主区域**：默认留白，仅当用户从侧边栏打开内容时，才以 **Tab 标签页** 形式展示已打开的页面。

## 二、布局结构

```
┌─────────────────────────────────────────────────────────────────┐
│  WorkBuddy 工作台      [全部启动] [全部关闭]    ◐  运行中        │
├──────────────┬──────────────────────────────────────────────────┤
│              │                                                  │
│  侧边栏      │              主区域 · 已打开页面                  │
│              │  ┌────────┬────────┬────────┬───────────────┐   │
│ 常用应用     │  │  tab1  │  tab2  │  tab3  │ + 新标签页     │   │
│  workbuddy   │  └────────┴────────┴────────┴───────────────┘   │
│  ima         │                                                  │
│  +添加应用   │  ┌────────────────────────────────────────────┐ │
│              │  │                                            │ │
│ 常用文件夹   │  │           页面内容 / iframe                │ │
│  📁 项目一   │  │                                            │ │
│    注释...   │  │                                            │ │
│  📁 项目二   │  │                                            │ │
│  +添加文件夹 │  │                                            │ │
│              │  └────────────────────────────────────────────┘ │
│ 常用文件     │                                                  │
│  示例在线表格│                                                  │
│  示例在线文档│                                                  │
│  +添加文件   │                                                  │
│              │                                                  │
└──────────────┴──────────────────────────────────────────────────┘
```

## 三、侧边栏设计

### 3.1 尺寸与位置
- 位置：固定左侧，高度占满视口（减去顶栏与底栏）。
- 宽度：260px（桌面端），允许横向轻微压缩但不换行。
- 背景：与页面整体背景一致，使用右侧细边框与主区域分隔。

### 3.2 内容分区（自上而下）

| 分区 | 内容 | 交互 |
|---|---|---|
| **常用应用** | 显示 `cfg.apps` 列表 | 点击整行 → 启动应用；右侧悬浮「编辑 / 删除」按钮 |
| **常用文件夹** | 显示 `cfg.pinnedFolders` | 点击整行 → 在右侧新 Tab 打开文件夹；右侧「打开 / 注释 / 取消固定」按钮 |
| **常用文件** | 显示 `cfg.files` | 点击整行 → 在右侧新 Tab 打开文件（在线 iframe / 本地提示） |

### 3.3 每个分区的统一组件

```
┌─────────────────────────────┐
│ 分区标题              + 添加 │
├─────────────────────────────┤
│ 💻 workbuddy        [编辑×] │
│ ─────────────────────────── │
│ 💻 ima              [编辑×] │
└─────────────────────────────┘
```

- 分区标题：小字大写风格（`letter-spacing: 0.08em`），右侧「+ 添加」按钮。
- 行内操作：默认隐藏，悬浮时显示（移动端始终显示）。
- 行高：44px，图标 + 名称 + 操作按钮。

## 四、右侧主区域设计

### 4.1 默认状态
- 不显示任何 Tab，中央显示引导文案：
  > 「点击左侧应用、文件夹或文件，在此处打开。」

### 4.2 打开后状态
- 顶部出现 **Tab 栏**，每个打开的内容一个 Tab。
- Tab 显示内容标题 + 关闭按钮「×」。
- 当前激活 Tab 高亮显示。
- Tab 下方为内容区：
  - 在线 URL：使用 `iframe` 嵌入（保留当前 `openUrlDirect` 能力，但改为在 Tab 内嵌）。
  - 本地文件：提示「已调用系统默认程序打开」。
  - 文件夹：提示「已打开文件夹」，并在需要时支持 iframe 内浏览（当前先以提示为主）。

### 4.3 Tab 行为
- 同一个项目不重复打开，点击已存在的 Tab 则切换过去。
- Tab 关闭后释放主区域空间。
- Tab 过多时支持横向滚动。

## 五、交互流程

1. 用户进入工作台（浏览器自动打开）。
2. 左侧侧边栏加载常用应用 / 文件夹 / 文件。
3. 用户点击任意侧边栏项目：
   - 应用 → 直接启动程序（不改变右侧 Tab）。
   - 文件夹 → 在右侧新增一个 Tab，标题为文件夹名/注释。
   - 文件 → 在右侧新增一个 Tab，标题为文件名。
4. 用户点击 Tab 切换内容。
5. 用户点击 Tab 的 × 关闭该页面。

## 六、美观度评估点

| 项目 | 当前方案 |
|---|---|
| 视觉层级 | 顶栏（品牌+全局操作）→ 左侧快捷栏 → 右侧内容区，层级清晰 |
| 信息密度 | 侧边栏紧凑，主区域留白，减少视觉疲劳 |
| 一致性 | 沿用现有 OKLCH 暖色 token、圆角、字体 |
| 响应式 | 桌面端 260px 侧边栏；窄屏可折叠为顶部横向快捷栏 |
| 微交互 | 悬浮显示操作按钮、Tab 切换高亮、打开内容 toast 反馈 |

## 七、可用性评估点

| 测试项 | 预期结果 |
|---|---|
| 点击应用 | 程序正常启动，toast 反馈 |
| 点击文件夹 | 右侧新增 Tab，文件夹打开无报警 |
| 点击在线文件 | 右侧新增 Tab，iframe 加载文档 |
| 点击本地文件 | 右侧新增 Tab，系统默认程序打开 |
| Tab 切换 | 点击 Tab 切换内容，当前 Tab 高亮 |
| Tab 关闭 | 点击 × 关闭，内容区回退到引导页 |
| 全部启动 | 只启动应用，不打开文件夹/文件 |
| 添加/删除/注释 | 现有功能完整保留 |
| 明暗主题 | 布局元素随主题切换颜色 |

## 八、实施范围

- **修改文件**：仅 `index.html`（视觉层 + 交互层）。
- **不修改**：`launcher.js`（后端路由与逻辑不变）、`config.json`（数据结构不变，沿用 `apps / pinnedFolders / files`）。
- **新增 JS 函数**：
  - `openInTab(item, type)`：在右侧主区域打开 Tab。
  - `switchTab(id)` / `closeTab(id)`：Tab 切换与关闭。
  - `renderSidebar()`：渲染左侧侧边栏。
  - `renderTabs()` / `renderTabContent()`：渲染 Tab 栏与内容区。

## 九、风险与处理

| 风险 | 处理 |
|---|---|
| 在线文档 iframe 拒绝嵌入（X-Frame-Options） | 提供「在新窗口打开」兜底按钮 |
| 本地文件夹无法 iframe 展示 | 仅显示「已打开文件夹」提示，并用 `explorer.exe` 打开 |
| 应用启动后右侧不应产生 Tab | 应用走 `openApp()`，不调用 `openInTab()` |
| 大量 Tab 导致 Tab 栏拥挤 | Tab 栏 `overflow-x: auto`，最小宽度 120px |

---

**下一步**：请评估以上方案。如无异议，我将按此设计实施到 `index.html`。
