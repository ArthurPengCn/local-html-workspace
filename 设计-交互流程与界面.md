# 工作台 · 交互流程与界面设计（基于 8/21 设计）

## 一、设计依据
- **8/21 原始需求**：三功能栏工作台 —— ①常用应用（可增删改）②常用文件夹（系统自动记录 + 固定/删除）③常用文件（本地 / 在线）。
- **8/21 既定风格**：本地启动服务（Node.js 零依赖）+ 单文件 HTML 页面，**暖色活力风格**。
- **现状偏差**：当前 `index.html` 为深色 navy 主题，与 8/21「暖色活力」不符；本设计将其拉回并固化，同时保留已落地的实用能力（搜索添加、文件夹固定/最近、在线文件弹窗、全局开关）。

## 二、信息架构
顶部品牌条 + 三栏等高卡片面板：
- **顶部**：品牌标识 + 全部启动 / 全部关闭 + 状态指示。
- **栏 1 常用应用**：搜索框 + 列表 + 添加入口。
- **栏 2 常用文件夹**：固定区 + 最近访问区。
- **栏 3 常用文件**：标签卡（本地 / 在线）+ 独立窗口。

## 三、交互流程

### 启动
双击 `start.bat` → 自动定位 node → 启动 `launcher.js`（端口 38760）→ 打开 `http://127.0.0.1:38760` → 若 `autostartApps=true`，依次拉起常用应用。

### 栏 1 · 常用应用
- 列表展示应用卡片（图标徽标 + 名称 + 路径 + 操作）。
- 单击「打开」→ 启动该应用。
- 顶部「全部启动」一次性拉起全部；「全部关闭」精确结束全部进程（含 UWP 按映像名兜底）。
- 「+ 添加应用」→ 弹窗手填名称 + 路径；或直接搜索框输入 → 实时搜本机应用 → 结果卡「添加」一键入列（免手填）。
- 每卡「编辑」改名称/路径、「删除」移除。
- 首次启动建索引时，搜索提示「正在建立应用索引…」。

### 栏 2 · 常用文件夹
- **固定文件夹**：单击 → 资源管理器打开（路径经环境变量传入，中文/空格/`&` 均无报警声）。
- **最近访问**：系统自动记录每次打开；单击打开；可「固定」（加入固定区）/「删除」。
- 「+ 添加文件夹」→ 选目录加入固定区。

### 栏 3 · 常用文件
- **本地文件卡**：单击打开。
- **在线文件（url）**：单击 → 工作台内以「标签卡」形式打开（iframe 渲染），带「独立窗口」按钮 → 弹出 `/popout.html` 极简独立窗（仅标题栏 + iframe，仅允许 http/https/ftp/ftps，非法 scheme 返回 400）。

### 全局反馈
- 底部状态栏：应用数、端口、下次刷新时间。
- 所有响应加 `Cache-Control: no-store`，避免旧缓存导致列表空白。

## 四、界面设计（暖色活力）
- **配色**：主色暖橙 `#FF7A3D`、活力珊瑚红 `#FF5E5B`、暖金 `#FFC857`；背景暖米渐变 `#FFF6EC → #FFE6CC`；卡片白底 + 软阴影 + 圆角 16。
- **布局**：顶部暖色渐变品牌条；下方三栏网格（≥900px 三栏并排，<900px 自动堆叠）。
- **组件**：卡片（图标徽标 + 名称 + 副信息 + 操作按钮）、搜索框、标签卡、模态（增改）、Toast、状态条。
- **可用性要点**：中文路径无报警、危险 scheme 拦截、`javascript:` 等返回 `{"ok":false}`、索引缓存使搜索瞬时返回。

## 五、与现有实现对照
| 项 | 现状 | 本设计 |
|----|------|--------|
| 风格 | 深色 navy | 暖色活力 |
| 布局 | 纵向三区堆叠 | 三栏并排卡片面板 |
| 搜索添加应用 | 已实现（已修复） | 沿用并强化 |
| 文件夹固定/最近 | 已实现 | 沿用 |
| 在线文件弹窗 | 已实现 | 沿用 |
| 全局开关 | 已实现 | 沿用置顶 |

## 六、交付物
- `design-prototype.html`：高保真暖色活力原型（示例数据，可演示搜索/标签卡/独立窗口交互）。
- 本说明文档。
- 若采纳，可将暖色主题 + 三栏布局应用到实际 `index.html`（后端 `launcher.js` 不变，零风险）。
